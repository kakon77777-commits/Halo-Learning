# Halo Learning v0.4.0 — B02 Reversible Renderer Retention Validation

Status: **resolved**

## Root cause

Real Chromium reports the renderer's own lifecycle mutations with a finer topology than the original mutation journal described:

1. `replaceWith(...children)` first removes child text nodes from the owned wrapper, then replaces that wrapper in its parent;
2. `Node.normalize()` mutates the surviving text node once per non-empty adjacent text node merged, while empty text nodes are only removed;
3. a standalone non-empty text node produces no normalization mutation.

The previous journal omitted the wrapper-side removal, collapsed multiple character-data records into one, and over-declared a mutation for a standalone text node. That produced two retained renderer records plus one pending expected descriptor even though the DOM itself restored correctly.

## Minimal repair

The renderer mutation journal now mirrors Chromium/DOM normalization topology without weakening the sanitizer or changing reversible DOM semantics. The focused unit fixture was updated to encode the observed normative record sequence.

Accepted repair commit: `cec727a66b43995373eeeec2971c96bca6c20ea1` (`fix: align renderer mutation journal with Chromium normalization`).

## Focused evidence

Local focused evidence recorded before the canonical rerun:

- `node --test tests/reversible-renderer.test.js tests/dynamic-dom-controller.test.js`
- result: `80/80 PASS`;
- real Chromium diagnostic of the failing normalization lifecycle: retained records `0`, pending descriptors `0`, source text and child shape restored exactly.

Canonical remote verification:

- source commit: `f1b413078763c1717ebd3a233cdfd71275592983`;
- Architect workflow run: `32994115568`;
- job: `browser-file (reversible-renderer)` / `98258768745`;
- focused real-Chromium result: **PASS**;
- same-run `core` job: **PASS**.

This satisfies the B02 prompt requirements: the focused browser case is green, reversible-renderer/unit-adjacent regression is green, exact restore behavior remains intact, and the mutation sanitizer drains renderer-owned records/descriptors instead of retaining the prior `2` / `[1]` residue.

## Disposition

B02 is **resolved**. No renderer redesign or acceptance-gate weakening was used.
