# Halo Learning v0.4.0 — Final Release Scope Correction

## Status

`REVALIDATION-PENDING`

This document applies the v0.4.0 final release closure policy to the historical convergence blockers. It records only classifications already supported by canonical product requirements and fresh evidence. It does **not** declare B08 complete and does not yet authorize merge to `main`.

## Canonical release contract

The v0.4 workbench contract requires the browser extension to satisfy the actual user/product path: real Chromium operation, dynamic DOM/SPA behavior, sensitive-site fail-closed behavior, production performance bounds, reversible rendering/cleanup, and a packaged production lexical runtime. Deterministic continuity specifically across `chrome.runtime.reload()` is not a stated v0.4 user-facing product guarantee.

The final closure therefore distinguishes product gates from convergence/evidence hardening instead of converting historical red evidence into artificial green evidence.

## B03 — `RESOLVED`

B03's non-negotiable product contract is sensitive-site security: sensitive pages must not be analyzed, extracted, rendered, stored, or remotely processed before the policy boundary denies them.

Fresh dedicated evidence:

- workflow: `v0.4.0 Final Closure — B03 Product Security`
- run: `33155664878`
- job: `98797700308`
- policy / fail-closed contracts: `31/31 PASS`
- installed Chromium product-security matrix: `1/1 PASS`
- canonical manifest remains host-permission-free and retains the minimal `activeTab`, `contextMenus`, `scripting`, and `storage` permission set.

The historical combined `tests/browser/sensitive-site.e2e.test.js` allowed-page lexical/network canary is not rewritten as PASS. Its allowed-page instrumentation problem is tracked separately from the sensitive-site security contract.

Final classification: **B03 = `RESOLVED`**.

## B06 — `RELEASE-DEBT`

The historical B06 checkpoint truthfully remains red for deterministic post-`chrome.runtime.reload()` continuity. That evidence is preserved and is not reclassified as a passing reload test.

The v0.4 product contract does, however, require ordinary installed MV3 runtime recovery to be healthy. Fresh closure evidence proves that path independently:

- workflow: `v0.4.0 Final Closure B06 MV3 Recovery`
- run: `33156336477`
- installed extension service worker is stopped through CDP;
- the normal installed native command wakes/restarts the worker;
- a fresh activated/running worker is observed;
- a fresh Halo runtime `networkActivity.lifetimeId` is observed.

Because ordinary installed runtime recovery is healthy and deterministic `chrome.runtime.reload()` continuity is not a frozen v0.4 user-facing guarantee, the unresolved dedicated reload continuity evidence is release debt rather than a product blocker.

Final classification: **B06 = `RELEASE-DEBT`**.

## B07 — `RESOLVED`

Runtime performance readiness is already closed by canonical B07 evidence. Fresh performance evidence selected the 64-bucket production runtime, and the checked-in production build receipt records `bucketCount: 64`, deterministic build output, and 128 packaged lexical shard files across the two language sets.

Final classification: **B07 = `RESOLVED`**.

## Allowed-page marking closure

The final release still requires a true allowed-page product smoke: the installed extension must actually perform local semantic enrichment and render Halo-owned token markup. Earlier closure diagnostics exposed two real defects rather than allowing an evidence-only downgrade:

1. packaged runtime injection was not idempotent per live tab; fixed by `0801879e6d8d0bdd472bdb986c722f09660c48bb`;
2. content/worker lexical identity was not truthfully synchronized across enrichment and bootstrap fallback; fixed by `83a6141cd6785c272247d2f4dbf1210dfc24dc78` after a full Node gate.

An exact-head installed-Chromium allowed-page revalidation is required after this document commit. Until that fresh run is green, release readiness is not declared.

## B08 — `EVIDENCE-BLOCKER`

B08 remains the final canonical evidence-envelope task. It must bind one exact canonical head to the scope-correct product gates, package/runtime evidence, explicit release debt, and final validator output.

Current classification: **B08 = `EVIDENCE-BLOCKER`** pending exact-head revalidation.

## Current release ruling

- product blockers: pending exact-head allowed-page revalidation only;
- B03: `RESOLVED`;
- B06 deterministic reload continuity: `RELEASE-DEBT`;
- B07: `RESOLVED`;
- B08: `EVIDENCE-BLOCKER`;
- `main_merge_allowed: false` until exact-head product gates and B08 final validation are complete.
