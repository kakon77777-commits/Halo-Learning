# Halo Learning v0.4.0 — Final Release Scope Correction

## Status

`CLOSURE-CANDIDATE`

The scope correction is complete. All product-critical v0.4.0 gates have fresh passing evidence on pre-closure source head `75017337b1f531b0f0d7c6afcd47bf623c3baf50`, B08 now has a canonical evidence envelope, and only the exact-head merge guard for the commit containing the final closure state remains.

This document does not authorize merge by itself. `main_merge_allowed` remains false until a fresh `v0.4.0 Final Release Revalidation` passes against the exact then-current integration HEAD containing the final validator, B08 envelope, final release validation record, and blocker ledger.

## Canonical release contract

The v0.4 workbench contract requires the browser extension to satisfy the actual user/product path: real Chromium operation, dynamic DOM/SPA behavior, sensitive-site fail-closed behavior, production performance bounds, reversible rendering/cleanup, trigger-controller behavior, accessibility, and a packaged production lexical runtime.

Deterministic continuity specifically across `chrome.runtime.reload()` is not a stated v0.4 user-facing product guarantee.

The final closure therefore distinguishes product gates from convergence/evidence hardening instead of converting historical red evidence into artificial green evidence.

Later v1.0+ conceptual expansion is explicitly out of scope for v0.4 closure.

## B03 — `RESOLVED`

B03's non-negotiable product contract is sensitive-site security: sensitive pages must not be analyzed, extracted, rendered, stored, or remotely processed before the policy boundary denies them.

Dedicated closure evidence established:

- workflow: `v0.4.0 Final Closure — B03 Product Security`;
- run: `33155664878`;
- job: `98797700308`;
- policy / fail-closed contracts: `31/31 PASS`;
- installed Chromium product-security matrix: `1/1 PASS`;
- canonical manifest remains host-permission-free and retains the minimal `activeTab`, `contextMenus`, `scripting`, and `storage` permission set.

The later pre-closure final workflow also passed the B03 product-security gate on exact source head `75017337b1f531b0f0d7c6afcd47bf623c3baf50`.

The historical combined `tests/browser/sensitive-site.e2e.test.js` allowed-page lexical/network canary is not rewritten as PASS. It remains explicit release debt.

Final classification: **B03 = `RESOLVED`**.

## B06 — `RELEASE-DEBT`

The historical B06 checkpoint truthfully remains red for deterministic post-`chrome.runtime.reload()` continuity. That evidence is preserved and is not reclassified as a passing reload test.

The v0.4 product contract does require ordinary installed MV3 runtime recovery to be healthy. Fresh closure evidence proves that path independently:

- workflow: `v0.4.0 Final Closure B06 MV3 Recovery`;
- run: `33156336477`;
- installed extension service worker is stopped through CDP;
- the normal installed native command wakes/restarts the worker;
- a fresh activated/running worker is observed;
- a fresh Halo runtime `networkActivity.lifetimeId` is observed.

The later pre-closure final workflow also passed ordinary installed MV3 recovery on exact source head `75017337b1f531b0f0d7c6afcd47bf623c3baf50`.

Because ordinary installed runtime recovery is healthy and deterministic `chrome.runtime.reload()` continuity is not a frozen v0.4 user-facing guarantee, the unresolved dedicated reload-continuity evidence is release debt rather than a product blocker.

Final classification: **B06 = `RELEASE-DEBT`**.

## B07 — `RESOLVED`

Runtime performance readiness is closed by canonical B07 evidence and fresh final revalidation.

On pre-closure source head `75017337b1f531b0f0d7c6afcd47bf623c3baf50`, final run `33170120169` passed the unchanged frozen performance gates on Chromium `151.0.7922.34`:

- 64 buckets: cold p95 `177.4 ms`, warm p95 approximately `0.1 ms`, long-task max `0 ms` — PASS;
- 128 buckets: cold p95 `129.4 ms`, warm p95 approximately `0.1 ms`, long-task max `0 ms` — PASS;
- fixed selection rule selected `64`.

The checked-in production build receipt records the deterministic selected 64-bucket runtime and 128 total packaged lexical shards.

Final classification: **B07 = `RESOLVED`**.

## Allowed-page marking closure

The installed extension must perform real local semantic enrichment and render Halo-owned token markup on an allowed page.

Closure diagnostics exposed and fixed two real defects rather than downgrading them:

1. packaged runtime injection was not idempotent per live tab; fixed by `0801879e6d8d0bdd472bdb986c722f09660c48bb`;
2. content/worker lexical identity was not truthfully synchronized across enrichment and bootstrap fallback; fixed by `83a6141cd6785c272247d2f4dbf1210dfc24dc78` after a full Node gate.

The final allowed-site marking product path passed on pre-closure exact source head `75017337b1f531b0f0d7c6afcd47bf623c3baf50` in final run `33170120169`.

## B08 — `RESOLVED`

B08 is now represented by the canonical evidence envelope:

`docs/validation/v0.4.0-b08-canonical-browser-evidence.md`

The envelope binds the scope-correct product browser gates, core/runtime evidence, performance, package/validator evidence, explicit release debt, and artifact identifiers/digests from the successful pre-closure exact-source final run.

Final classification: **B08 = `RESOLVED`**.

## Pre-closure final revalidation

Exact source head:

`75017337b1f531b0f0d7c6afcd47bf623c3baf50`

Workflow run:

`33170120169` — **SUCCESS**

Passed jobs:

- core `98844935815`;
- product-browser `98844935873`;
- trigger-controller `98844935770`;
- performance `98844935682`;
- release-package-validator `98844935775`.

## Current release ruling

```yaml
product_blockers: []
evidence_blockers:
  - exact-head-final-merge-guard
release_debt:
  - b06-deterministic-runtime-reload-continuity
  - legacy-combined-sensitive-site-allowed-network-canary
  - legacy-worker-b-e-workflow-noise
B03: RESOLVED
B06: RELEASE-DEBT
B07: RESOLVED
B08: RESOLVED
main_merge_allowed: false
```

## Final merge guard

The exact Git commit containing the completed scope correction, B08 evidence envelope, final validation record, scope-correct validator, and final blocker ledger must receive a fresh successful `v0.4.0 Final Release Revalidation`.

After that exact-head run succeeds, and only if the integration branch does not move before merge, PR #4 may be marked release-ready and merged to `main` without another source change.
