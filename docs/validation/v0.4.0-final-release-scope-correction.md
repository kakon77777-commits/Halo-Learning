# Halo Learning v0.4.0 — Final Release Scope Correction

## Status

`RELEASE-READY`

The v0.4.0 release scope correction is complete. Product-critical browser/runtime/security/package gates have fresh passing evidence, B08 has a canonical evidence envelope, and later v1.0+ concepts remain explicitly out of scope.

A successful exact-head closure validation was obtained at source head `6c8bfd3974fee51186361de2a0da9eac6f9d4582` with `v0.4.0 Final Release Revalidation` run `33170635672`; all five final jobs passed. The final ledger commit still requires its own exact-head run before merge, but no further product or architecture change is authorized.

## Canonical v0.4 product contract

The release requires:

- real Chromium operation;
- dynamic DOM/SPA behavior;
- sensitive-site fail-closed behavior;
- allowed-site local semantic enrichment/marking;
- ordinary installed MV3 recovery;
- reversible rendering/cleanup;
- trigger-controller behavior;
- accessibility;
- frozen performance bounds;
- deterministic packaged production lexical runtime;
- development and standalone final validators.

Deterministic continuity specifically across `chrome.runtime.reload()` is not a frozen v0.4 user-facing product guarantee.

## Final blocker disposition

### B03 — `RESOLVED`

Sensitive-site privacy/security is product-critical and passed dedicated installed-Chromium evidence plus the final exact-head product-browser lane. The production manifest remains host-permission-free. The historical combined allowed-page network canary is preserved separately as release debt and is not rewritten as PASS.

### B06 — `RELEASE-DEBT`

Ordinary installed MV3 recovery is a v0.4 product gate and passed fresh final evidence. Historical deterministic `chrome.runtime.reload()` continuity evidence remains red and visible in `docs/validation/v0.4.0-b06-mv3-reload-lifecycle.md`; it is release debt, not a product PASS and not a v0.4 product blocker.

### B07 — `RESOLVED`

The selected production runtime is the deterministic 64-bucket configuration. Frozen performance gates passed in final validation; selector semantics were not weakened.

### B08 — `RESOLVED`

Canonical evidence envelope:

`docs/validation/v0.4.0-b08-canonical-browser-evidence.md`

It binds product-browser, security, runtime, performance, package, validator and release-debt evidence.

## Final validated closure source

Exact source head:

`6c8bfd3974fee51186361de2a0da9eac6f9d4582`

Workflow:

- `v0.4.0 Final Release Revalidation`
- run `33170635672`
- result: **SUCCESS**

Jobs:

- core `98846648094` — PASS
- product-browser `98846648149` — PASS
- trigger-controller `98846648193` — PASS
- performance `98846648146` — PASS
- release-package-validator `98846648131` — PASS

## Release ruling

```yaml
product_blockers: []
evidence_blockers: []
release_debt:
  - b06-deterministic-runtime-reload-continuity
  - legacy-combined-sensitive-site-allowed-network-canary
  - legacy-worker-b-e-workflow-noise
B03: RESOLVED
B06: RELEASE-DEBT
B07: RESOLVED
B08: RESOLVED
main_merge_allowed: true
```

`main_merge_allowed: true` is subject to the immutable merge guard: the exact final integration HEAD containing the release-ready ledger must itself pass `v0.4.0 Final Release Revalidation`, and the branch must not move between that successful run and PR #4 merge.

## Scope boundary

This decision closes Halo Learning v0.4.0 only. No v0.5 or v1.0+ work is authorized by this document.
