# Halo Learning v0.4.0 — B08 Canonical Browser Evidence Envelope

## Status

`RESOLVED` as the canonical v0.4.0 browser/release evidence envelope.

This document does not waive the final merge guard: the exact Git commit containing this envelope, the final validator contract, and the closure ledger must itself receive a successful `v0.4.0 Final Release Revalidation` before PR #4 may merge to `main`.

## Scope-correct release contract

B08 binds the v0.4.0 product-critical browser/runtime gates after the final release scope correction. It intentionally distinguishes real product acceptance from historical evidence-harness debt.

Later v1.0+ concepts are out of scope for this envelope.

## Pre-closure exact-source proof

The final closure harness was repaired and verified at source head:

`75017337b1f531b0f0d7c6afcd47bf623c3baf50`

Canonical workflow:

- workflow: `v0.4.0 Final Release Revalidation`
- run: `33170120169`
- conclusion: **SUCCESS**

All final jobs passed:

- `core` — job `98844935815` — PASS
- `product-browser` — job `98844935873` — PASS
- `trigger-controller` — job `98844935770` — PASS
- `performance` — job `98844935682` — PASS
- `release-package-validator` — job `98844935775` — PASS

## Product browser evidence

The final product-browser lane passed all scope-correct v0.4.0 gates on the same source head:

1. B03 sensitive-site product security — PASS
2. allowed-site marking product path — PASS
3. B06 ordinary installed MV3 recovery — PASS
4. twenty-class browser runtime matrix — PASS
5. reversible renderer — PASS
6. accessibility — PASS

The focus-sensitive trigger-controller lifecycle is intentionally isolated in its own final job. Its unit regression and installed-browser lifecycle both passed.

## B03 disposition

B03 is **RESOLVED** for the v0.4.0 product contract.

Fresh product-security evidence demonstrates that sensitive sites fail closed before private extraction/annotation, and the production manifest remains host-permission-free.

The historical combined sensitive-site/allowed-page network canary remains visible as release debt; it is not rewritten as PASS.

## B06 disposition

The ordinary installed MV3 recovery product path is accepted and passed final revalidation.

The historical deterministic `chrome.runtime.reload()` continuity proof remains **RELEASE-DEBT**. That historical red evidence is preserved in:

`docs/validation/v0.4.0-b06-mv3-reload-lifecycle.md`

No synthetic PASS, unsafe extension debugging, arbitrary sleep-as-readiness, or production permission widening is claimed.

## B07 / performance evidence

The final performance job passed the unchanged frozen gates on Chromium `151.0.7922.34`.

Final measurements from run `33170120169`:

| Candidate | Cold p95 | Warm p95 | Long-task max | Result |
| --- | ---: | ---: | ---: | --- |
| 64 | 177.4 ms | ~0.1 ms | 0 ms | PASS |
| 128 | 129.4 ms | ~0.1 ms | 0 ms | PASS |

The frozen selection rule remained unchanged and selected **64**.

The canonical selected production runtime remains bound to:

- bucket count: `64`
- shard count: `128`
- manifest hash: `8d5e11ccf28f4872879ed84444d99c317969892bacfc1048110c6243f56e3d92`
- root hash: `50bd7b032a48cb0536e2aff5138e70014edc510baf3afabec053f180efd21031`

## Core / package / validator evidence

The same final run passed:

- full Node regression;
- canonical lexical runtime verification;
- semantic quality regression;
- release tooling regression;
- source hygiene;
- deterministic release package built twice and byte-compared;
- final development validator;
- standalone source extraction without `.git` metadata;
- final standalone validator;
- selected production lexical runtime package audit.

Final release artifacts from run `33170120169`:

- performance artifact `9685272693`, digest `sha256:bc7f6c947726c792450308bc0d8c494ccf449debe3e73f5ea8e13dc9ba8e1542`
- release artifact `9685268121`, digest `sha256:b391d95a2da2bcbbcf4b57888a352e29fbeb79e50cd485868432be708a82a799`

## Explicit release debt

The v0.4.0 release carries the following visible debt:

1. deterministic `chrome.runtime.reload()` continuity proof on the governed CI host;
2. the legacy combined sensitive-site / allowed-page network canary;
3. legacy Worker B/E push workflows remain noisy beside the canonical final-release workflow.

None of these items is being represented as a passing product gate.

## B08 ruling

B08 is **RESOLVED** because the canonical envelope now truthfully binds the product-critical browser, runtime, performance, package, security, and validator evidence while preserving release debt as debt.

## Final merge guard

`self` means the Git commit containing this document.

PR #4 may become merge-ready only when a fresh `v0.4.0 Final Release Revalidation` against the exact then-current integration HEAD (including this envelope and the final closure ledger) concludes **SUCCESS**.

Until that exact-head guard passes, `main_merge_allowed` remains false.
