# Halo Learning v0.4.0 — Final Release Validation Record

## Status

`CLOSURE-CANDIDATE`

This record captures the successful pre-closure exact-source final revalidation and defines the final merge guard for the commit containing the canonical B08 envelope, scope correction, validator contract, and blocker ledger.

## Pre-closure validated source

Source head:

`75017337b1f531b0f0d7c6afcd47bf623c3baf50`

Final workflow:

- `v0.4.0 Final Release Revalidation`
- run `33170120169`
- conclusion: **SUCCESS**

Jobs:

| Gate | Job | Result |
| --- | --- | --- |
| core | `98844935815` | PASS |
| product-browser | `98844935873` | PASS |
| trigger-controller | `98844935770` | PASS |
| performance | `98844935682` | PASS |
| release-package-validator | `98844935775` | PASS |

## Product gate summary

Passed on one exact source head:

- full Node regression;
- canonical lexical runtime;
- semantic quality;
- source hygiene;
- B03 sensitive-site product security;
- allowed-site local semantic marking;
- B06 ordinary installed MV3 recovery;
- browser runtime matrix;
- reversible renderer;
- accessibility;
- trigger-controller units and installed-browser lifecycle;
- frozen 64/128 performance comparison;
- deterministic release package twice with byte comparison;
- development final validator;
- standalone extraction / no Git metadata;
- standalone final validator;
- selected production lexical runtime package audit.

## Performance

Chromium: `151.0.7922.34`

- 64 buckets: cold p95 `177.4 ms`, warm p95 approximately `0.1 ms`, long-task max `0 ms` — PASS
- 128 buckets: cold p95 `129.4 ms`, warm p95 approximately `0.1 ms`, long-task max `0 ms` — PASS
- frozen selection rule selected `64`.

No frozen threshold or selector semantics were changed.

## Package / artifact evidence

Run `33170120169` published:

- final performance artifact `9685272693`
  - digest `sha256:bc7f6c947726c792450308bc0d8c494ccf449debe3e73f5ea8e13dc9ba8e1542`
- final release artifact `9685268121`
  - digest `sha256:b391d95a2da2bcbbcf4b57888a352e29fbeb79e50cd485868432be708a82a799`

Production lexical runtime remains:

- selected bucket count: `64`
- deterministic build: `true`
- shard count: `128`
- manifest hash: `8d5e11ccf28f4872879ed84444d99c317969892bacfc1048110c6243f56e3d92`
- root hash: `50bd7b032a48cb0536e2aff5138e70014edc510baf3afabec053f180efd21031`

## Scope-correct blocker disposition

```yaml
product_blockers: []
evidence_blockers:
  - final exact-head merge guard until closure-candidate commit passes
release_debt:
  - b06-deterministic-runtime-reload-continuity
  - legacy-combined-sensitive-site-allowed-network-canary
```

B03 is resolved by dedicated product-security evidence.

B06 ordinary installed MV3 recovery is accepted. Historical deterministic `chrome.runtime.reload()` continuity evidence remains red and is preserved as release debt, not rewritten as PASS.

B07 is resolved with the selected production 64-bucket runtime.

B08 is resolved by `docs/validation/v0.4.0-b08-canonical-browser-evidence.md`.

## Final exact-head merge guard

`self` means the Git commit containing the complete final closure state.

Before PR #4 may merge:

1. the integration branch must include this record, the B08 evidence envelope, final scope-correction state, final validator contract, and final blocker ledger;
2. `v0.4.0 Final Release Revalidation` must run against that exact integration HEAD;
3. every job in that run must conclude SUCCESS;
4. no collaborator may move the branch after that successful run and before merge;
5. PR #4 head must equal the validated integration HEAD.

Until those conditions are satisfied, `main_merge_allowed` remains false.

## Scope boundary

This validation closes Halo Learning v0.4.0 only. Later v1.0+ concepts are not part of this release decision.
