# Halo Learning v0.4.0 — B07 Runtime Performance Readiness Evidence

- Branch: `integration/v0.4.0-final-convergence`
- Blocker: `B07_runtime_performance`
- Prompt: `09_B07_RUNTIME_PERFORMANCE_READINESS_PROMPT.md`
- Result: **RESOLVED**
- Final promoted-source validation HEAD before closure docs: `90aff0b14f6c9d4234c75c8735e48a1360d0daa2`
- Production promotion commit: `56380fb8a3434ebfe83f40d232e7b1355f162d16`

## Scope and collaborator-aware continuation

This closure remained restricted to B07. A collaborator had already landed the first architecture hypothesis at `b2b51813b92daecf74c563fd028d74a77a9c7d4c` (`perf: move shard invariants to build attestation for B07`). Rather than reimplementing that work, this workbench verified it with fresh canonical Chromium evidence, completed the production promotion required by the B07 contract, and revalidated the promoted source state.

No second architecture hypothesis was needed.

## Baseline blocker

The frozen Chromium baseline failed the B07 gates:

- 64 buckets: cold required-shards p95 `529.0 ms`, warm lookup p95 `0.1 ms`, long-task max `70 ms` — blocked.
- 128 buckets: cold required-shards p95 `339.4 ms`, warm lookup p95 `0.2 ms`, long-task max `0 ms` — blocked.

Frozen gates were unchanged:

- cold required-shards p95 `<= 300 ms`;
- warm lookup p95 `<= 100 ms`;
- main-thread long-task max `<= 50 ms`.

## Architecture hypothesis 1

Commit `b2b51813b92daecf74c563fd028d74a77a9c7d4c` moves repeated runtime shard invariant work to build-time attestation while preserving serialized content hashing, root-hash/integrity binding, provenance, and verification contracts. The optimization changes where trusted invariant validation is paid; it does not weaken the frozen gates or accept unverifiable shard content.

## First fresh real-Chromium verification

Evidence trigger commit: `8dbce6a72202f4035f37bad3afcc38e07b193010`.

Architect run `33063093362`, performance job `98486456371`: **PASS** on Chromium 151.

Fresh comparison selected 64 by the frozen rule (`64 if both pass; 128 if only 128 passes; blocked if neither passes`). Both candidates passed all blocking gates. Representative measurements from this first post-hypothesis run:

- 64: cold p95 `209.9 ms`, warm p95 `0.1 ms`, long-task max `0 ms`;
- 128: cold p95 `135.6 ms`, warm p95 `0.2 ms`, long-task max `0 ms`.

This established that hypothesis 1 crossed the performance gate. It did not by itself close B07 because the prompt also requires the selected candidate to become the production and packaged runtime.

## Production promotion

A bounded one-time promotion workflow was staged at `0bb50020b321ce1c9c8c4f2959f6503a232a49a5` and executed as run `33063662768`, job `98488356874`.

The workflow passed every fail-closed step before publication:

1. fresh frozen Chromium 64/128 comparison;
2. require selected bucket count = 64 and all frozen gates PASS;
3. write canonical raw comparison evidence;
4. rebuild selected 64 runtime and bind the selected candidate manifest/root hashes to the built tree;
5. functional-equivalence, integrity, runtime and quality regressions;
6. package the release twice and byte-compare extension ZIP, source ZIP, and package manifest;
7. inspect the packaged extension for `data/lexical-v0.4.0/manifest.json` and the selected shard tree;
8. fetch/reconcile remote state and refuse publication if another collaborator moved the branch;
9. remove the one-time workflow;
10. normal non-force publication.

All steps passed. The workflow published commit `56380fb8a3434ebfe83f40d232e7b1355f162d16` (`perf: promote selected 64-shard runtime for B07`).

Canonical production artifacts now include:

- `docs/validation/v0.4.0-browser-shard-comparison.json`;
- `apps/extension/data/lexical-v0.4.0/`;
- `dist/lexical-v0.4.0/`.

The selected production data manifest records:

- `selectionStatus: selected-by-browser-comparison`;
- `bucketCount: 64`;
- selected manifest hash `8d5e11ccf28f4872879ed84444d99c317969892bacfc1048110c6243f56e3d92`;
- selected root hash `50bd7b032a48cb0536e2aff5138e70014edc510baf3afabec053f180efd21031`;
- shard count `128`;
- source/provenance metadata preserved.

The extension service-worker production path already targets `data/lexical-v0.4.0/manifest.json`; after this promotion it resolves the selected runtime instead of falling back because the canonical selected tree is now present and packaged.

## Final promoted-source canonical revalidation

Because the promotion commit was emitted by `GITHUB_TOKEN`, it did not recursively trigger push workflows. Evidence-only marker commit `90aff0b14f6c9d4234c75c8735e48a1360d0daa2` explicitly triggered a fresh Architect run against the source state that already contains the promoted runtime.

Architect run `33064002580` produced the required B07 closure evidence.

### Performance — PASS

Job `98489495011`: **PASS**, Chromium `151.0.7922.34`, sample count `5` per candidate.

Final raw summary:

| Candidate | Cold p50 | Cold p95 | Cold max | Warm p50 | Warm p95 | Warm max | Long p50/p95/max | Result |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |
| 64 | 175.5 ms | 198.8 ms | 198.8 ms | 0.1 ms | 0.2 ms | 0.2 ms | 0 / 0 / 0 ms | PASS |
| 128 | 115.3 ms | 120.6 ms | 120.6 ms | 0.1 ms | 0.2 ms | 0.2 ms | 0 / 0 / 0 ms | PASS |

All three frozen gates were true for both candidates. The fixed selection rule again selected **64**. The performance evidence artifact was uploaded as `architect-v0.4.0-performance-33064002580-1` (artifact ID `9642970224`).

### Core — PASS

Job `98489494652`: **PASS**.

- Full Node regression: PASS
- Canonical lexical runtime: PASS
- Semantic quality regression: PASS
- Worker E focused regression: PASS
- Worker C release-tooling regression: PASS

### Release evidence — PASS

Job `98489494973`: **PASS**.

- isolated release evidence workspace: PASS;
- deterministic candidate built twice and byte-compared: PASS;
- standalone source extraction with Git metadata absent: PASS;
- development validator: PASS;
- extracted standalone validator: PASS;
- evidence artifact upload: PASS.

### Adjacent runtime evidence

Same promoted-source run:

- `browser-file (browser-runtime-matrix)`: PASS;
- accessibility: PASS;
- reversible-renderer: PASS.

Failures in trigger-controller, sentence-pipeline, dynamic-dom, sensitive-site, and MV3 lanes remain owned by other blocker/frontier states. They are not performance regressions and are not reclassified as B07 failures.

## Acceptance decision

**B07 is RESOLVED.**

The first architecture hypothesis crosses every frozen performance gate in fresh real Chromium; the fixed rule selects 64; that selected configuration is now bound to canonical raw evidence, promoted into the production extension runtime, included in deterministic packages, and covered by functional/provenance/integrity and standalone-validator evidence.

No frozen threshold was weakened. No benchmark-only candidate is being mistaken for production. No second architecture hypothesis was consumed.

Remaining blocker frontier after B07 closure:

- B03: blocked;
- B06: blocked;
- B08: open/dependent on unresolved browser evidence, including B06.

The overall v0.4.0 release remains blocked and merge to `main` remains prohibited.
