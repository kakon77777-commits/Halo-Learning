# Halo Learning v0.4.0 — Worker C Independent Acceptance Matrix

**Worker:** C — Independent Verification & Release Engineering
**Repository:** `kakon77777-commits/Halo-Learning`
**Required common seed:** `handoff/v0.4.0-pause-seed @ a83f38aa63a38aa2eaf7ac75aea80659cbe4834c`
**Worker branch:** `parallel/v0.4.0-verification`
**Snapshot date:** 2026-08-26
**Release under verification:** `v0.4.0 — Browser Runtime & UX`

## Scope and interpretation

This matrix is an independent Worker C evidence map. It does **not** declare v0.4.0 complete and does not treat Worker A or Worker B completion claims as release evidence. The Worker C branch starts from the frozen common seed and contains release-verification tooling/tests only. Cross-worker acceptance must be rerun after the Architect integrates A, B, and C.

Status vocabulary:

- **C-GREEN** — freshly verified by Worker C for Worker-C-owned release tooling in this execution session.
- **PENDING-INTEGRATION** — an evidence path exists or is being produced by another worker, but Worker C has not independently rerun it on the integrated tree.
- **BLOCKER** — a frozen v0.4 release requirement is not presently satisfied on the Worker C branch / current execution surface.

## Acceptance matrix

| Frozen v0.4 requirement | Evidence path / gate | Worker C status | Classification / note |
| --- | --- | --- | --- |
| Dynamic DOM / SPA lifecycle | `tests/dynamic-dom-controller.test.js`; `tests/browser/dynamic-dom.e2e.test.js` | PENDING-INTEGRATION | Runtime evidence must be rerun after Worker A integration and in real Chromium where applicable. |
| Reversible / idempotent rendering | `tests/reversible-renderer.test.js`; `tests/browser/reversible-renderer.e2e.test.js` | PENDING-INTEGRATION | Existing seed evidence is mapped; no independent integrated rerun yet. |
| Trigger correctness | `tests/trigger-controller.test.js`; `tests/content-trigger-runtime.test.js`; `tests/browser/trigger-controller.e2e.test.js` | PENDING-INTEGRATION | Existing seed evidence is mapped; no independent integrated rerun yet. |
| Sensitive-site fail-closed behavior | `tests/site-policy.test.js`; `tests/content-policy-lifecycle.test.js`; `tests/browser/sensitive-site.e2e.test.js`; Worker C privacy/security static gate | PENDING-INTEGRATION | Static verifier is C-GREEN in focused tests; sensitive-site browser proof still requires integration + Chromium. |
| Accessibility / resilient presentation | `tests/accessibility-contract.test.js`; `tests/browser/accessibility.e2e.test.js` on Worker B branch | BLOCKER | Not present in the common-seed/C tree at Worker C snapshot; must be integrated and independently rerun. |
| 20-class browser fixture matrix | `fixtures/browser/`; `tests/browser/fixture-matrix.e2e.test.js` | BLOCKER | Required frozen evidence is absent from the common-seed/C tree at Worker C snapshot. |
| Real browser performance | `scripts/profile-browser-runtime.js`; required `docs/validation/v0.4.0-browser-baseline.json`; required `docs/validation/v0.4.0-browser-shard-comparison.json` | BLOCKER | Profiler exists, but required fresh real-Chromium evidence files are not available on the C tree. |
| MV3 service-worker lifecycle | `tests/browser-service-worker-cdp.test.js`; browser lifecycle evidence | PENDING-INTEGRATION | Unit/CDP evidence path exists; final browser lifecycle proof requires integrated Chromium run. |
| Standalone release validation | `scripts/validate-v0.4.0.js`; `tests/release-validator-v0.4.0.test.js` | C-GREEN / PENDING-INTEGRATION | Worker C synthetic extracted-root/no-Git path is GREEN; final extracted integrated release audit is still required. |
| Package integrity / deterministic release packaging | `scripts/package-v0.4.0.js`; `tests/release-packaging-v0.4.0.test.js` | C-GREEN / PENDING-INTEGRATION | Worker C deterministic/integrity tests are GREEN; final integrated artifacts must be built and audited. |

## Worker C fresh evidence

Fresh command:

```text
node --test tests/release-validator-v0.4.0.test.js tests/release-packaging-v0.4.0.test.js
```

Fresh result:

```text
tests 24
pass 24
fail 0
cancelled 0
skipped 0
todo 0
```

Additional fresh checks on all four Worker C source/test artifacts:

```text
node --check: PASS
UTF-8 decode: PASS
trailing-whitespace scan: PASS
actual merge-conflict-marker scan: PASS
```

The release packager regression also reproduced and then fixed one Worker-C-owned determinism defect: an already-generated v0.4 package sidecar could otherwise enter the next source ZIP and alter the repeated full-release hash. The canonical packager now excludes its own prior sidecar from source-package inventory, and repeated `packageRelease()` output is covered by regression.

## Current BLOCKER classification

### BLOCKER C-B01 — v0.4 release metadata not yet integrated

The frozen common seed still exposes v0.3 package/extension release metadata and v0.3 validation/package scripts. Worker C intentionally does not rewrite cross-worker release identity on its isolated verification branch merely to make its verifier pass. The Architect must integrate the v0.4 tree and establish the final v0.4 metadata boundary before final validation.

### BLOCKER C-B02 — required browser acceptance evidence not yet integrated / freshly verified

The current Worker C tree does not contain a completed 20-class fixture matrix or the required final browser-performance evidence, and Worker C has not independently produced a fresh real-Chromium acceptance run on an integrated A/B/C tree. Under the frozen contract, Node-only evidence cannot substitute for this gate.

### BLOCKER C-B03 — Worker C execution environment cannot perform the final full-repository browser audit

This sandbox can reach the repository through the connected GitHub interface, but direct `git clone` / registry network access is unavailable in the local execution container. Therefore Worker C could verify its owned tooling locally and push it remotely, but cannot honestly claim a fresh full-repository Node + Playwright + Chromium release audit from this environment.

This is an execution-environment blocker to Worker C final sign-off, not evidence of a new Halo production-runtime defect.

## RELEASE-DEBT

None added by Worker C.

## FUTURE-HARDENING

None added by Worker C. No acceptance requirement was expanded beyond the frozen v0.4 contract.

## Architect integration gate

After integrating Worker A, Worker B, and Worker C, run the final validation from the integration branch with exact locked dependencies and a genuine supported Chromium executable. Only the Architect/Integrator may convert the release status to complete after all frozen gates are freshly GREEN.
