# Halo Learning v0.4.0 — B05 Trigger Controller / Panel Evidence

- Branch: `integration/v0.4.0-final-convergence`
- Blocker: `B05_trigger_controller_panel`
- Prompt: `07_B05_TRIGGER_CONTROLLER_PANEL_PROMPT.md`
- Result: **BLOCKED — bounded repair budget exhausted**
- Starting source HEAD: `bdc20369cc603d82f9b6ce00b8432aafd0dba83e`
- Source HEAD after round 2: `392a2c67995925d9b582b8b70b63e43648df0075`
- Repair rounds consumed: `2 / 2`

## Scope

This continuation was restricted to B05. It did not reopen convergence, change `main`, alter sensitive-site policy, broaden extension permissions, or begin B06/B07/B08 work.

## Baseline RED

Canonical Architect run `32996197609`, job `98266020920` (`browser-file (trigger-controller)`) reproduced the retained B05 failure:

```text
1 test
0 pass
1 fail
page.waitForSelector: Timeout 30000ms exceeded
waiting for locator('[data-halo-owned="panel"]') to be visible
location: tests/browser/trigger-controller.e2e.test.js:91
```

A dedicated pre-repair diagnostic run `32996197405`, job `98266019346`, established that Chromium registered the `halo-analyze-selection` command with `Alt+Shift+H` and selected the expected active tab, but Playwright `page.keyboard.press('Alt+Shift+H')` did not exercise the browser-level extension shortcut. After the synthetic key event the content receiver was absent and the panel count was zero.

## Causal finding

The original B05 browser test treated a renderer-level Playwright keyboard event as if it were a browser/OS-level extension shortcut. That did not establish the native command lifecycle or its user-gesture/`activeTab` path. The trigger-controller and browser-entry unit surfaces were already green, so the first bounded repair targeted the test input boundary rather than product policy or runtime semantics.

## Repair round 1 — native shortcut causal experiment

Commits:

- `f32b01edf15df0c3b1013808c400d978f0d17943` — drive the diagnostic through X11/`xdotool`
- `f90366a4540018945c4636400812ed573488a068` — assert the native shortcut reaches the panel

CI run `32998966552`, job `98275440514`: **PASS**.

The headed Chromium/Xvfb diagnostic sent a real X11 `Alt+Shift+H` event. Fresh evidence showed:

- the expected `halo-analyze-selection` command and shortcut were registered;
- the active tab was the fixture page;
- content `HALO_STATUS` became reachable after the native shortcut;
- one selection read and one renderer call were observed;
- `lastError` was null;
- panel count after the native shortcut was exactly `1`.

This proved that the native browser-command path is automatable and materially advanced the failure beyond the original synthetic-key boundary.

## Repair round 2 — canonical installed-browser lane

Commits:

- `42e5d64688e568c7bbf6edbb1edf21036039f5d6` — add an opt-in X11 native-shortcut driver to the browser harness
- `392a2c67995925d9b582b8b70b63e43648df0075` — isolate the canonical B05 trigger-controller lane under headed Chromium/Xvfb

The generic headless browser matrix no longer owns `trigger-controller`; the dedicated lane installs `xdotool`, runs the trigger/browser-entry unit regression, and then runs the original `tests/browser/trigger-controller.e2e.test.js` with `HALO_NATIVE_SHORTCUT_DRIVER=xdotool`. The original E2E test was not skipped or replaced.

Architect run `32999533292`, job `98277398112` produced:

### Unit regression

```text
28 tests
28 pass
0 fail
```

This includes browser-entry packaging/registration/error containment plus trigger mode, explicit-only, dismissal, reentry, timer, and cancellation behavior.

### Installed-browser lifecycle

```text
1 test
0 pass
1 fail
page.waitForSelector: Timeout 30000ms exceeded
waiting for locator('[data-halo-owned="panel"]') to be visible
64 × locator resolved to hidden <div data-halo-owned="panel"></div>
location: tests/browser/trigger-controller.e2e.test.js:91
```

The second round therefore changed the observed failure boundary: unlike the original no-receiver/no-panel diagnostic, the canonical lifecycle now reaches a real panel DOM node, but that panel remains hidden instead of becoming visible.

The runner also reported that its window manager does not support `_NET_ACTIVE_WINDOW`; the native driver falls back to locating a visible Chromium X11 window. Because the lifecycle subsequently resolves the Halo panel element itself, the remaining failure cannot truthfully be closed as the original synthetic-key problem alone.

## Bounded-repair ruling

B05 is **not resolved**. Two causal repair rounds have been consumed. The residual blocker is now narrower:

> In the canonical installed-browser lifecycle, the native shortcut reaches a state where `[data-halo-owned="panel"]` exists, but the panel does not transition to visible before the 30-second assertion deadline.

The responsible visibility/state-transition cause was not isolated and repaired within the authorized two-round budget. A third code repair is explicitly not attempted in this continuation.

## Preserved boundaries

No B05 repair changed:

- `apps/extension/manifest.json` permissions or host permissions;
- service-worker production command semantics;
- trigger-controller production FSM;
- panel renderer production semantics;
- sensitive-site policy or user meta-control;
- any other blocker state.

## Continuation requirement

Any further B05 repair requires a new explicit authorization beyond the exhausted two-round budget. The next investigation should start from the new residual observation — **panel exists but remains hidden in the canonical native-command lifecycle** — rather than returning to the already disproven synthetic-key hypothesis.
