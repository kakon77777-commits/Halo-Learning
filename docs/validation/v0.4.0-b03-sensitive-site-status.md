# Halo Learning v0.4.0 — B03 Sensitive-site Installed-extension Status Checkpoint

Status: **BLOCKED — two causal rounds exhausted**

Restart anchor: `a1a128977bcc6853368231872c118390b24c8803`

Authorized prompt: `06_B03_SENSITIVE_SITE_STATUS_PROMPT.md`

## Historical blocker

The fresh closure failure was the first installed-extension command timing out while `statusFor()` could not obtain a `HALO_STATUS` response:

```text
Halo content status unavailable
```

The frozen security invariant remained unchanged: sensitive sites must fail closed, no private content may be read or annotated, and status must not expose sensitive content.

## Source inspection and first causal round

Tracing the command path showed that Chromium had registered `halo-analyze-selection` with shortcut `Alt+Shift+H`, and the expected tab was active, but Playwright's page-level synthetic keyboard event did not trigger the browser-level `chrome.commands.onCommand` event in headless Chromium. The content receiver therefore never existed and `chrome.tabs.sendMessage()` returned `Could not establish connection. Receiving end does not exist.`

A deterministic test candidate then verified the registered shortcut and called the already-registered production `handleCommand()` directly. Command, policy, and fail-closed unit tests remained green (`31/31`), but the real installed-extension case returned `false` before content status became available.

The additional evidence established why: a direct service-worker call does not carry Chrome's `activeTab` user-gesture grant. The production command path relies on that grant before `chrome.scripting` injection. Therefore direct invocation was not a valid semantic substitute for a genuine command event and was not accepted.

First-round workflow evidence:

- run `32995367747`;
- command/policy/fail-closed contracts: `31/31 PASS`;
- focused real Chromium: FAIL at `registered production command handler must dispatch to the active tab`.

## Second causal round

The second bounded candidate kept production code and the canonical manifest unchanged. The focused test created a temporary extension copy, first asserted the canonical manifest had no `host_permissions`, and added host permissions only to the temporary test copy for the exact fixture/service hosts exercised by this test. This supplied the headless test with the authority that a real Chrome user gesture would normally grant, while continuing through the production handler → packaged injection → content runtime → policy → status path.

Fresh evidence from B03 one-shot run `32995877994`, job `98264839319`:

- test-only patch application: PASS;
- command/policy/fail-closed contracts: `31/31 PASS`;
- the original `Halo content status unavailable` point was passed;
- production command dispatch returned true and content status became available;
- the focused browser file then failed later at the allowed lexical canary:

```text
allowed lexical marking must observe manifest plus shard fetch attempts
```

Thus the original status-unavailability symptom was advanced, but the required `sensitive-site.e2e` file did not reach PASS. The second candidate was not committed by the one-shot workflow.

## Security disposition

No production permission expansion was accepted. The canonical `apps/extension/manifest.json` remains host-permission-free. No fail-closed policy gate was weakened, and the policy/negative contract suite remained green in both bounded rounds.

## Budget ruling

The master burn-down contract allows at most two causal repair rounds and states:

```text
If two rounds fail:
STOP → keep BLOCKER → publish checkpoint
```

Both rounds were consumed without satisfying the required focused browser PASS. B03 therefore remains **BLOCKED**. No third B03 repair round is authorized in this series.

The next exact unresolved boundary is no longer the original missing receiver/status point; it is the interaction between the headless command-authority model and the allowed lexical-network accounting used by the sensitive-site integrated test. Reopening that boundary requires a separately authorized continuation rather than silently extending this prompt's repair budget.
